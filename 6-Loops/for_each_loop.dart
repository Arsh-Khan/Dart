// forEach loop
main()
{
  var numbers = [1,2,3,4];
  
  // forEach is higher order function which has function inside a function
  //(n) => print(n) is also a function
  numbers.forEach((n) => print(n));
  // => Arrow operator
}