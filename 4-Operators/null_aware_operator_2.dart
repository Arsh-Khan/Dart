main() {
  // int number;
  var number;
  // ??= null aware operator
  print(number ??= 100);
  // if the number is null then assign the default value to it
  // it will be assigned permanently

  print(number);
}
